import React from 'react';
//  initial the project is scafolded out on a basic level to allow for tinkering later 
const Display = ({gameOver, text}) => (
    <div>{text} </div>
)

export default Display;
 